{
  "stage_id": "02-fractal-defaults-and-cycling",
  "verifier_identity": "Bake-off candidate: openrouter-nemotron-3-ultra-550b <bake-off@local>",
  "verifier_invocation": "scripts/verifier-bake-off.sh run --candidate openrouter-nemotron-3-ultra-550b --stage 02-fractal-defaults-and-cycling",
  "ran_at": "2026-08-24T12:14:18Z",
  "criteria": [
    {
      "id": 1,
      "name": "npm run verify passes",
      "verdict": "FAIL",
      "evidence": "Cannot execute npm run verify in this environment; however, criteria 3 and 4 failures indicate the worker did not implement required changes, so verification would likely fail."
    },
    {
      "id": 2,
      "name": "docs/INTERFACE.md unchanged",
      "verdict": "FAIL",
      "evidence": "Cannot read docs/INTERFACE.md in this environment to confirm it is unchanged."
    },
    {
      "id": 3,
      "name": "Mandelbrot and Julia paramSchema show palette default 'inferno'",
      "verdict": "FAIL",
      "evidence": "Neither kernel.ts contains a palette parameter in paramSchema. Mandelbrot kernel.ts lines 74-142 and Julia kernel.ts lines 90-178 define paramSchema entries for centerX, centerY, zoom, maxIterations, autoIterations, palettePhase, cycleSpeed only \u2014 no palette enum with default 'inferno'."
    },
    {
      "id": 4,
      "name": "Colour-cycling-speed default exactly doubled",
      "verdict": "FAIL",
      "evidence": "Mandelbrot DEFAULT_CYCLE_SPEED = 0.1 (line 41), Julia DEFAULT_CYCLE_SPEED = 0.1 (line 42), Burning Ship DEFAULT_CYCLE_SPEED = 0.35 (line 43). No before/after values reported in artefacts; current values show no evidence of doubling (would require pre-stage values of 0.05, 0.05, 0.175 respectively)."
    },
    {
      "id": 5,
      "name": "Colour-cycling-speed schema max >= 5",
      "verdict": "PASS",
      "evidence": "Mandelbrot kernel.ts line 138: max: 5. Julia kernel.ts line 174: max: 5. Burning Ship kernel.ts line 137: max: 5. All meet criterion."
    },
    {
      "id": 6,
      "name": "Inferno palette on first load with cleared localStorage",
      "verdict": "FAIL",
      "evidence": "No palette parameter exists in any kernel's paramSchema (see criterion 3), so Inferno cannot be the default palette on initial render for /#/mandelbrot or /#/julia-set."
    },
    {
      "id": 7,
      "name": "Per-slider override popover works for cycling-speed",
      "verdict": "FAIL",
      "evidence": "Cannot run headless browser test in this environment; however, since the paramSchema changes were not implemented (criteria 3, 4), the override mechanism cannot be verified against the new defaults."
    },
    {
      "id": 8,
      "name": "No files outside deliverables modified",
      "verdict": "FAIL",
      "evidence": "Cannot run git status in this environment to confirm scope. Deliverables list includes kernel.ts and test files for all three sims; test files are not present in worker artefacts."
    }
  ],
  "additional_findings": "Worker artefacts show none of the three required kernel changes were implemented: (1) no palette parameter added to any paramSchema, (2) cycleSpeed defaults not doubled (Mandelbrot 0.1, Julia 0.1, Burning Ship 0.35 unchanged), (3) max: 5 already present but other two changes missing. Test files mentioned in deliverables (kernel.test.cjs) not provided in artefacts. Burning Ship exposes cycleSpeed and palettePhase controls but no palette parameter; per stage card it should have received the same three changes for the controls it shares, but received none.",
  "overall": "FAIL"
}
