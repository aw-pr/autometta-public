## Monitoring finding

**Routine:** <!-- check-public-mirror | check-brew-install | check-upstream-skills -->
**Script exit code:** <!-- 0 = pass, 1 = fail, 2+ = error -->
**Run time (UTC):**

### Script output

```
<!-- paste full script stdout here, truncated to 1000 characters if large -->
```

### Operator checklist

- [ ] No state mutation occurred (`state/`, `state.yaml`, and verifier artefacts are untouched)
- [ ] Finding is understood (or closed as expected behaviour)
