---
name: decision-verifier-handoff-naming
description: The verifier handoff artefact is named verifiers.
metadata:
  type: project
---

The verifier handoff artefact is named `verifiers`.

**Why:** This resolves the verifier handoff naming question in `docs/philosophy.md`.

**How to apply:** Use `verifiers` as the handoff name in loop-layer conventions and keep it aligned with [[decision-loop-name-phat-controller]].
